<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ExamenProgreso3EncaladaD.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:ExamenProgreso3EncaladaD.Views">

    <TabBar>
        <ShellContent Title="Registro"
                      ContentTemplate="{DataTemplate views:RegistroClientePage}"
                      Route="RegistroClientePage" />

        <ShellContent Title="Clientes"
                      ContentTemplate="{DataTemplate views:ClientesListPage}"
                      Route="ClientesListPage" />

        <ShellContent Title="Logs"
                      ContentTemplate="{DataTemplate views:LogsPage}"
                      Route="LogsPage" />
    </TabBar>

</Shell>
